// Stonecutter controller: settings.gradle + stonecutter.gradle.
// Toolchain plugins are provided by buildSrc convention plugins (mightyarchitect.loom-*, .shadow)
// and applied in the branch scripts: common/fabric/neoforge build.gradle.
