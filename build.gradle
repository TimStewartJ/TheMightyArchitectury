// Stonecutter controller: settings.gradle + stonecutter.gradle.
// Toolchain plugins come from the buildSrc convention plugins (mightyarchitect.loom-remap,
// .loom-noremap, .moddev) and are applied in the branch scripts: common/fabric/neoforge
// build.gradle. There is one buildscript per loader and no shared multiloader framework.
